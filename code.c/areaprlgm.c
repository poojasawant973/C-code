//Find the area off parallelogram
#include <stdio.h>
int main()
{
	int height, width, area;
	printf("Enter the value of width and height");
	scanf("%d%d", &width, &height);
	area = width * height;
	printf("%d", area);
	return 0;
}
