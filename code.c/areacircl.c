// Construct a flowchart to caluculate the area of a circle. 
#include <stdio.h>
int main()
{
	int r, Area;
	printf("Enter the radius of a circle");
	scanf("%d", &r);
	Area = 3.14 *(r * r);
	printf("Area of a circle : %d", Area);
	return 0;
}
	
	
