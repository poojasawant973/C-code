// Calculate the flowchart to calculate the square off a square
#include <stdio.h>
int main()
{
	int N, squ;
	printf("Enter the number");
	scanf("%d", &N);
	squ = N * N;
	printf("square is : %d", squ);
	return 0;
}
	
