// Display qutotient of no.

#include <stdio.h>
int main()
{
	int N, L, quotient;
	printf("Enter the value of N");
	scanf("%d", &N);
	quotient= N / L;
	printf("quotient is :%d",quotient);
	return 0;
}
